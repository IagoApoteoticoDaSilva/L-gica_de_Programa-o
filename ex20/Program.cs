// Enunciado: Desenvolva um programa que utilize um laço `for` para imprimir os números de 0 a 20, incrementando de 2 em 2, um em cada linha

for(int z = 0; z <= 20; z+=2)
{
    Console.WriteLine(z);
}